# 默认基础配置 · AideanAgentFleet 基线（2026-09-13）

> 本文件是舰队"出厂默认值"的唯一权威快照。任何改动作出后应同步更新此文件。
> AI IDE 修改配置前先读这里，改完在这里登记。

---

## 1. 端口分配

| 端口 | 归属 | 说明 |
|---|---|---|
| 5000 | 控制台 | fleet\console\console.py（零依赖 stdlib） |
| 9900 | Manager（default profile） | A2A 入口 + 编排，**零 MCP**（极速派工） |
| 9901 | worker-a 前端一组 | |
| 9902 | worker-b 后端一组 | |
| 9903 | worker-c 测试工程师 | |
| 9907 | pm-1 产品经理 | |
| 9908 | fe-2 前端二组 | |
| 9909 | be-2 后端二组 | |
| 9910 | be-3 后端三组 | |
| 9911 | reviewer-1 审查员 | start-fleet v3 起自动拉起 |
| 5410 | 演示：贪吃蛇 API | Test0912 |
| 5411 | 演示：贪吃蛇页面 | Test0912 |
| 10808 | 本地代理（外部） | be 系三角色的 b.ai 出网依赖它 |

新角色端口从 **9912** 起由控制台自动分配。

## 2. 模型矩阵

| 角色 | base_url | model_id | 代理 | Key 变量名（在对应 .env） |
|---|---|---|---|---|
| Manager | https://api.gpt.ge/v1 | gpt-6-astra | 无 | HERMES_CUSTOM_API_GPT_GE_API_KEY（2026-09-14 xiaohongshu_create 正式任务切换，前值 agnes-3.0-flash，回退备份 `fleet\configs\config.manager.bak-before-v3-20260914.yaml`） |
| pm-1 | https://apihub.agnes-ai.com/v1 | agnes-3.0-flash | 无 | HERMES_CUSTOM_API_PM_1_KEY |
| reviewer-1 | https://apihub.agnes-ai.com/v1 | agnes-3.0-flash | 无 | HERMES_CUSTOM_API_REVIEWER_1_KEY |
| worker-a | https://token.sensenova.cn/v1 | sensenova-6.8-flash-lite | 无 | HERMES_CUSTOM_API_SENSENOVA_API_KEY |
| fe-2 | https://token.sensenova.cn/v1 | sensenova-6.8-flash-lite | 无 | HERMES_CUSTOM_API_FE_2_KEY |
| worker-b | https://api.b.ai/v1 | qwen3.8-flash | http://127.0.0.1:10808 | HERMES_CUSTOM_API_BAI_API_KEY |
| be-2 | https://api.b.ai/v1 | qwen3.8-flash | 同上 | HERMES_CUSTOM_API_BE_2_KEY |
| be-3 | https://api.b.ai/v1 | qwen3.8-flash | 同上 | HERMES_CUSTOM_API_BE_3_KEY |
| worker-c | https://api-inference.modelscope.cn/v1 | deepseek-ai/DeepSeek-V4-Flash-0731 | 无 | HERMES_CUSTOM_API_MODELSCOPE_API_KEY |
| 全员替补 | https://integrate.api.nvidia.com/v1 | nvidia/nemotron-3-ultra-550b-a55b | 无 | HERMES_CUSTOM_API_NVIDIA_API_KEY（仅 worker-a/b/c 有 `fallback_providers` 兜底 + 真实 .env 变量；其余 5 个 profile 无 fallback 段，NVIDIA 变量在其 .env 为 MISSING——注意这与名册“全员兜底”口径不一致，待对齐） |

规则：
- 所有 Key 只存 `.env`；config.yaml 用 `${VAR}` 引用；MCP `env:` 块的 `${VAR}` 会被插值（值来自**该角色自己**的 profile `.env`——exa 的 `${EXA_API_KEY}` 每个 profile 各存一份，不是共用主 .env）。
- `.env` 里只能写真实值，**禁止 `${VAR}` 字面量**（dotenv 不做插值——2026-09-13 事故：代理写成 `${HTTP_PROXY}` 导致 be 系 401）。
- 无重复键、无 `${` 字面量通过真机复核（2026-09-13）。
- 每个角色实际消费的 Key 以其 config `${VAR}` 引用为准：pm-1→PM_1_KEY、fe-2→FE_2_KEY、be-2→BE_2_KEY、be-3→BE_3_KEY、reviewer-1→REVIEWER_1_KEY、worker-a→SENSENOVA、worker-b→BAI、worker-c→MODELSCOPE。另各 profile 另行引用 APIFREE_KEY1（辅助模型/兜底）与 TERMINAL_MODAL_IMAGE。
- 换模型 = 改 profile config.yaml 的 model 块（`default/base_url/api_key/api_mode` 4 行）+ 重启该网关 + 烟测；查有效模型 ID：`curl -H "Authorization: Bearer <key>" <base_url>/models`。
- Manager 正式大任务建议 gpt-6-astra（api.gpt.ge，Key 变量 `HERMES_CUSTOM_API_GPT_GE_API_KEY`，当前 FILL_ME），3 行切换见方案文档 §2。

## 3. 通用基座（所有 worker 的工具集策略）

- **保留**：browser, web, file, terminal, code_execution, memory, session_search, todo, clarify, context_engine
- **裁剪**（写入各 profile `agent.disabled_toolsets`）：computer_use, connections, cronjob, delegation, discord, discord_admin, homeassistant, image_gen, skills, spotify, stt, tts, video, video_gen, vision, x_search, yuanbao
- **Skills**：worker 端保持移除（免费档 TPM 实测 22KB 索引会打爆配额）；备份在 `fleet\configs\skills-backup-*`
- **MCP**（仅员工侧，Manager `mcp_servers:` 为空段零 MCP）：8 个 worker/reviewer profile 各挂 chrome-devtools + sequential-thinking + exa（`EXA_API_KEY: ${EXA_API_KEY}` 插值，值来自各 profile 自身 .env）；context7 全域已移除（主 + 8 profile 零残留，真机复核）
- 快照存档：`fleet\console\state\基座快照-20260912.md`

## 4. 职能提示词模板

控制台按职能关键词自动套用（改职能保存即重生成，版本存 `fleet\console\state\souls\<id>-vN.md`）：

| 职能关键词 | 模板 | 要点 |
|---|---|---|
| 产品/需求/经理/策划 | PM | 产出 PRD+可验收标准，不写代码 |
| 前端/UI/界面/页面 | FE | 契约调用接口、零依赖、自测贴证据 |
| 后端/API/服务端/数据 | BE | 端口铁律、输入校验、启停自清理 |
| 测试/QA/质量/联调 | QA | 只测不改、真实执行、原样记录失败 |
| （其余） | 通用员工/审查/观察模板 | 按 ftype |

## 5. 流程门禁（六道门）

1. 类型化流程权限：worker/reviewer/observer 三选一，派工只列 worker、送审只列 reviewer
2. 状态机硬门：DRAFT→ASSIGNED→DOING→SUBMITTED→REVIEWING→DONE/PARTIAL/REWORK/BLOCKED，非法迁移拒绝
3. 机器门：`grep:相对路径:令牌` 模式（UTF-8 直读）或白名单命令，**退出码非 0 不得 PASS**
4. 目录白名单：E:\Demo 与 fleet\projects
5. 命令黑名单：taskkill/del/format 等 20+ 模式
6. 派工只经 Manager（hermes --yolo → a2a_call），网页/IDE 不直调 Worker

## 6. 容错基线

| 场景 | 行为 |
|---|---|
| 模型 429（hermes 内部） | 不重试，直接中止（源码分类为不可重试） |
| 派工遇 429（控制台层） | 自动退避 60s→120s 重试 2 次，审计留痕，仍败转 FAILED |
| 派工空回执 | 重试 1 次 |
| 角色网关离线 | 派工/送审直接拦截（先探活） |
| 角色模型挂 | NVIDIA 兜底（worker 侧 fallback_providers）；Manager 无兜底（agnes 挂则人工重发） |
| 高危动作 | 必停等用户批准 |
| ESCALATED | 返工 3 次上限后上报，不静默 |

## 7. 文件位置表

| 内容 | 路径 |
|---|---|
| 控制台源码 | fleet\console\console.py |
| 控制台状态 | fleet\console\state\（roster/tasks/projects/audit/evidence/souls/基座快照） |
| 启动脚本 | fleet\start-fleet.ps1 · fleet\console\start-console.ps1 |
| 项目脚手架 | fleet\tools\new-project.ps1 |
| 迁移工具 | fleet\tools\export-fleet.ps1 / import-fleet.ps1 |
| Manager 章程 | C:\Users\EDY\AppData\Local\hermes\SOUL.md（副本 fleet\configs\manager-SOUL.md） |
| Worker SOUL | ...\profiles\<id>\SOUL.md（模板 fleet\configs\worker-SOUL-template.md） |
| 审计流水 | ...\hermes\a2a_audit.jsonl + fleet\console\state\audit.log |
| 六节报告 | fleet\reports\ |
| 方案总文档 | 实施方案-Hermes-A2A多Agent系统.md |

## 8. 当前占位（FILL_ME / MISSING，真机核验 2026-09-13）

| 变量 | 状态 | 用途 | 不填的后果 |
|---|---|---|---|
| HERMES_CUSTOM_API_GPT_GE_API_KEY | 9 个 .env 全 FILL_ME | 切回 gpt-6-astra 时用 | 不影响当前运行；想切 gpt-6 时必填 |
| HERMES_CUSTOM_API_APIFREE_KEY1 | 8 个 profile 全 FILL_ME，主 .env 无此行 | Manager/各 profile 辅助模型（apifree 视觉/压缩/标题）与兜底 | 缺失时自动回退主模型；执行报告称“8/8 就绪”即此状态 |
| HERMES_CUSTOM_API_NVIDIA_API_KEY | 仅 worker-a/b/c 的 .env 有 FILL_ME 行，其余 6 处 MISSING（连行都没有） | worker-a/b/c 的 `fallback_providers` 兜底 | 兜底当前不可用；主模型正常时无影响 |
| be 系代理（HTTP_PROXY/HTTPS_PROXY/NO_PROXY） | be-2/be-3/worker-b 三个 profile 均为真值；主 .env 无此三行 | b.ai 出网（be 系走 10808 代理） | be 系改动此三行即 401；主 .env 不需要它们 |

已恢复为真值的（切勿再动 export/import 的默认脱敏会清掉它们）：主 AGNES/EXA/OPENROUTER；pm-1 PM_1_KEY；fe-2 FE_2_KEY；be-2 BE_2_KEY；be-3 BE_3_KEY；reviewer-1 REVIEWER_1_KEY；worker-a SENSENOVA；worker-b BAI；worker-c MODELSCOPE；worker-c 的 EXA（刚从主 .env 同步）。

# 启动说明 · AideanAgentFleet 舰队操作手册

> 配套：[AGENTS.md](AGENTS.md)（AI IDE 流程规则）· [默认基础配置.md](默认基础配置.md)（端口/模型/门禁基线）
> 本文回答三件事：**怎么启动、怎么日常用、怎么搬到别的电脑/别的项目**。

---

## 一、启动与停止

### 一键启动（11 个进程）
```powershell
powershell -File E:\Code\AideanCompany\fleet\start-fleet.ps1
```
拉起：控制台(5000) + Manager(9900) + 8 员工网关(9901-9911，含 reviewer-1) + 贪吃蛇双服务(5410/5411)。

### 启动后自检（3 条命令）
```powershell
netstat -ano | Select-String "9900|9901|9902|9903|9907|9908|9909|9910|9911"
curl.exe http://127.0.0.1:5000/api/health          # 控制台健康，应 20/20
curl.exe http://127.0.0.1:9900/.well-known/agent-card.json   # 应返回 Hermes-Manager
```

### 停止
- 逐个关闭各网关窗口；Manager 用 `hermes gateway stop`；紧急全停 `hermes pause`。
- 注意 `start-fleet.ps1` 还拉起了贪吃蛇演示服务（node server.js + `python -m http.server 5411`）——关机/清端口时别漏这俩（不需要时可注释掉脚本末尾两行）。

### ⚠ 重启前检查（重要）
1. 控制台「角色」页逐个【连通测试】，8/8 就绪才重启网关/电脑；
2. 主/员工 `.env` 中模型 Key 行不得含 `FILL_ME`——当前**故意留占位**的三项见[默认基础配置.md §8](默认基础配置.md)（GPT_GE/APIFREE/NVIDIA 兜底，不影响运行；其余 Key 行必须是真值）。

---

## 二、日常使用（开发一个项目的标准动作）

### 开新项目（1 分钟）
```powershell
cd E:\Code\AideanCompany\fleet\tools
.\new-project.ps1 -Name "项目名" -Dir "E:\Demo\目录名"
```
自动建目录骨架 + README 契约模板 + git + 注册进控制台项目页。**目录必须在 `E:\Demo` 或 `fleet\projects` 白名单内。**

### 填契约（10 分钟，唯一需要动脑的）
填 `README.md`：目标 / 分工铁律 / 接口契约 / **验收命令**（推荐 `grep:相对路径:令牌1,令牌2`）/ 明确不做。

### 派活（两种）
- **全自动**：`hermes --yolo chat` 对 Manager 说目标（测试期用 agnes：串行、小任务；大任务前把 Manager 切 gpt-6-astra，3 行切换见方案文档 §2）。
- **手动挡**：控制台「任务」页建任务 → 派工 → 机器验收 → 送审 → 收口，每步有拦截门。

### 收结果
- 六节报告：`fleet\reports\`；进度/耗时/ETA/token：控制台「项目」页；全程留痕：「审计」页。
- 你只处理：ESCALATED / BLOCKED 条目 + 高危动作批准 + 成品体验判断。

---

## 三、导出到其他电脑（跨机迁移）

### 旧电脑（1 条命令）
```powershell
E:\Code\AideanCompany\fleet\tools\export-fleet.ps1
# 默认导出到桌面 fleet-export-<日期>.zip；加 -WithKeys 才带密钥（仅建议 U 盘直传，不走云）
```
包含：fleet 全套（控制台/脚本/SOUL/state/审计）、9 个 profile 的 config+SOUL、Manager config+SOUL、演示项目、MANIFEST（含原机路径记录，导入时自动改写）。**默认已脱敏全部 Key**（导出时会再扫描一遍所有文本文件）。

### 新电脑（5 步）
1. 装前置：Node.js LTS、Python 3.11+、Git、Hermes Agent v0.21.1（与原机同版）；
2. 验证：`hermes --version; node --version; python --version; git --version`；
3. 导入（路径按需改，默认 E:\Code）：
   ```powershell
   .\import-fleet.ps1 -Zip "$env:USERPROFILE\Desktop\fleet-export-xxxx.zip" -Root "E:\Code"
   ```
   首次导入会生成 `FILL_ME` 骨架 .env —— 按「角色」页清单回填各 Key（从你的供应商仪表盘取值）；
4. 启动并验证：`start-fleet.ps1` → `curl.exe http://127.0.0.1:5000/api/health`；
5. 逐角色连通测试。

### 导入安全机制（PROTECT MODE）
- 目标机已有带真 Key 的 hermes home 时，导入器**拒绝触碰任何 .env/config.yaml**（防覆盖事故）；
- 支持 `-DryRun` 先演练一遍；
- 导出脱敏覆盖所有文本文件（不分扩展名），导入前再验一次零泄漏。

### 不随迁移走的（设计如此）
Hermes 程序本体、API Key 值、10808 代理（be-2/be-3 需新机本地代理或改 .env）、ModelScope 当日配额（跟账号）、MCP 的 npx 缓存（首次使用自动重下）。

---

## 四、导出到其他项目（同机复用）

舰队是机器级共享的——**新项目不需要重建任何角色**。只需：
1. `new-project.ps1` 建项目骨架（见二）；
2. 填 README 契约；
3. 开 chat 说目标。

若项目需要新职能（如"UI 设计师""数据工程师"）：控制台「角色」页【新增角色】——职能名称自由填写，流程权限三选一（执行/审查/观察），自动建 profile+SOUL 模板+端口+同步通讯录。

---

## 五、故障排查速查

| 症状 | 处理 |
|---|---|
| 派工被拦"网关离线" | 角色页【启动】该角色（控制台会主动拦假在线） |
| `a2a_call` 工具不可见 | `hermes tools enable a2a` 后 `hermes tools list` 确认 |
| 429 | 控制台自动退避 60s→120s 重试 2 次；手动等 2 分钟；ModelScope 日配额尽则次日 |
| 401 Authentication failed | 先 `curl` 直测该供应商 Key；再查该角色 .env 的键名与 config `${VAR}` 是否一致；be 系检查 10808 代理 |
| 控制台 5000 无响应 | 看 console 窗口报错；常见是 state JSON 被手改坏 |
| 网关起了但名片 404 | 等 30-60 秒（并发启动时 MCP 子进程拉起慢） |
| 修了 config/.env 不生效 | config 改动需重启对应网关；`.env` 对新会话自动生效 |

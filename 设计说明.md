# Agent Fleet 设计说明

| 项 | 内容 |
|---|---|
| 项目名 | Agent Fleet（工作名） |
| 版本 | v1.0（设计基线，对应《需求文档.md》v1.0） |
| 日期 | 2026-09-11 |
| 技术基线 | Python 3.11+ 自研薄 Control Plane + 可插拔 Coding Agent Runtime + 确定性工程执行层 + Evidence-first 验收体系 |
| 设计输入 | `初始设计/d0.md ~ d8.md` |

---

## 1. 架构总览

### 1.1 系统定义

> Agent Fleet 是一个针对 Coding Agent（未来：通用 AI Worker）的轻量级执行控制面：**契约驱动、事件溯源、资源隔离、策略恢复、证据验收**。Kernel 不感知任何具体领域（coding/media）与任何具体基座（Claude Code/Codex/...）。

### 1.2 分层架构

```text
┌────────────────────────────────────────────────────────┐
│  UI 层   Streamlit 三按钮 + 配置页（可替换，核心不依赖）      │
├────────────────────────────────────────────────────────┤
│  CLI 层  factory run / resume / cancel / report        │
├────────────────────────────────────────────────────────┤
│  Control Plane（Kernel，自研，~3k 行）                     │
│                                                        │
│  WorkflowEngine   Scheduler     PolicyEngine   GateEngine│
│  (DAG 解释执行)   (并发/冲突预检)  (容错状态机)    (机器验收门链) │
│  ResourceManager  EventBus      StateStore    ArtifactStore│
│  (Worktree/Lease) (append-only)  (SQLite 投影)  (证据归档)  │
│         SemanticVerifier（LLM，仅解释事实，无翻案权）         │
├────────────────────────────────────────────────────────┤
│  Worker Runtime（插件）                                   │
│  AgentWorker: claudecode / codex / opencode / pi / ...  │
│  ToolWorker:  shell / python（V1）                        │
│  HumanWorker: approval 节点（V1）                          │
├────────────────────────────────────────────────────────┤
│  Domain Pack（插件）                                      │
│  coding/（MVP）  media/（P2）  research/（P2+）             │
│  各自提供：域 Gate、skills、任务模板、acceptance 语义        │
├────────────────────────────────────────────────────────┤
│  Storage                                                │
│  state.db(SQLite)  events.jsonl  artifacts/  reports/   │
└────────────────────────────────────────────────────────┘
```

### 1.3 一次运行的数据流

```text
TaskPack(.yaml)
  → PackLint（Schema 校验）
  → Planner：解析为 DAG（静态 write_set 冲突检测 → 必要时串行化）
  → Scheduler：就绪任务入并发池（先申请 Worktree + Resource Lease）
  → AgentWorker：拼装 prompt(system+taskpack+retry_context)
      → 子进程调用基座 CLI（流式读取 JSON 事件，增量落盘）
  → CollectEvidence：diff、命令输出、EventStream 帧 → artifacts/
  → GateEngine：Diff→Path→Port→Command/Test→Contract（确定性）
       PASS → SemanticVerifier（LLM 判断+叙述）→ Merge Gate → SUCCEEDED
       FAIL → PolicyEngine 判定：返工(带 RetryContext)/重试/拆分/升级
  → 全部终态 → ReportEngine：模板渲染 report.html（纯事实数据）
```

---

## 2. 架构决策记录（ADR，冻结）

| ADR | 决策 | 复审条件 |
|---|---|---|
| ADR-001 | 不用 MetaGPT/CrewAI/LangGraph/Mastra/OpenClaw 做核心 Orchestrator | 出现需要跨进程持久工作流引擎的需求且自研成本>迁移成本 |
| ADR-002 | Python 3.11+ 自研薄 Control Plane | — |
| ADR-003 | Claude Code / Codex / OpenCode / pi 全部视为 Worker Backend | — |
| ADR-004 | CLI 差异只能存在于 Adapter 层 | — |
| ADR-005 | 确定性恢复由 Policy Engine 执行，Manager LLM 不处理机械重试 | — |
| ADR-006 | Machine Gate 权威高于 Agent 自述；LLM 可解释失败，不可篡改事实层结果 | — |
| ADR-007 | Evidence immutable / raw-first；report.html 从事实数据生成 | — |
| ADR-008 | 每个并发写任务独立 git worktree；共享资源用 Lease | — |
| ADR-009 | SQLite 存状态（投影），JSONL 存事件（事实），filesystem 存 artifact/evidence | — |
| ADR-010 | MVP UI 用 Streamlit，核心代码不依赖 Streamlit | — |
| ADR-011 | MCP 是未来工具协议，不代替内部 Scheduler/Event Bus | — |
| ADR-012 | OpenClaw 保留给未来运营/客服/常驻层，不进入当前核心依赖 | — |
| ADR-013 | Kernel 不 import 任何 Domain/基座符号；依赖方向 `domains→core`、`workers→core`，反向禁止 | — |
| ADR-014 | MVP 只交付 coding Domain；抽象正确性以 P2 阶段 media Domain "不改 core 跑通"为验收 | — |

---

## 3. 模块划分与目录结构

```text
ai_fleet/
├── core/                      # Kernel：不知道 pytest/Claude/微信公众号是什么
│   ├── workflow/              # DAG 解释器：ready 计算、依赖解除、conditional、retry loop
│   ├── scheduler/             # 并发池、write_set 冲突预检、串行化降级
│   ├── events/                # EventBus：append-only JSONL 写入 + 投影更新
│   ├── state/                 # StateStore：SQLite，任务/运行状态机
│   ├── policy/                # PolicyEngine：429/400/timeout/blocked 决策
│   ├── artifacts/             # ArtifactStore：证据归档、内容寻址
│   ├── evidence/              # Evidence 模型与编号
│   ├── gates/                 # Gate Protocol + 通用门（schema/budget/human_approval）
│   ├── resources/             # Lease 表、端口探测、进程树管理
│   ├── workers/               # Worker Protocol + Capabilities + 注册表
│   ├── verify/                # SemanticVerifier（LLM 调用，输入证据，输出判断）
│   └── report/                # 模板渲染（Jinja2），只读 events/artifacts
├── workers/                   # 具体 Worker 实现（依赖 core 接口）
│   └── agents/
│       ├── claude_code.py     # claude -p --output-format stream-json ...
│       ├── codex.py           # codex exec --json ...
│       ├── opencode.py        # opencode run ...
│       └── pi.py              # pi 无头模式（便宜模型通道）
├── domains/
│   └── coding/                # MVP 唯一 Domain Pack
│       ├── gates/             # PortGate / DiffGate / PathGate / TestGate / ContractGate
│       ├── skills/            # backend-test / pnpm-vitest / playwright-e2e ...
│       ├── prompts/           # 员工A/B/C、verifier 的 system prompt
│       └── templates/         # 任务包模板
├── skills/                    # 公司级 skills（brand/security/approval）
├── storage/                   # 运行时数据（runs/<run_id>/...）
├── ui/                        # Streamlit 壳（只调 CLI/SDK，不含逻辑）
├── cli.py                     # factory run/resume/cancel/report/lint
└── tests/                     # FakeAdapter、故障注入、门单测
```

**依赖方向铁律**（编译期可用 import-linter 强制）：

```text
domains ────────→ core
workers ────────→ core（接口）
core ──✗──→ coding / media / claude_code / opencode / pytest / git
```

---

## 4. 核心数据模型与协议

### 4.1 organization.yaml（组织与角色）

```yaml
version: 1
organization:
  name: AideanStudio

roles:
  employee-A:
    display: 员工A-后端
    worker: { type: agent, backend: claudecode, model: claude-sonnet-4-5 }
    fallback_models: [claude-haiku-4-5, gpt-5-mini]
    system_prompt: prompts/employee-A.md
    skills: [backend-test, ruff-mypy, api-contract]
    timeout_s: 900
    max_retries: 2
    budget: { per_task_usd: 2.0 }

  employee-B:
    display: 员工B-后端契约
    worker: { type: agent, backend: claudecode, model: claude-haiku-4-5 }   # 同基座不同模型
    fallback_models: [gpt-5-mini]
    system_prompt: prompts/employee-B.md
    skills: [pytest, sse-contract]

  employee-C:
    display: 员工C-前端
    worker: { type: agent, backend: opencode, model: qwen3-coder-plus }
    fallback_models: [gpt-5-mini, deepseek-v3.2]      # 经 pi 通道
    system_prompt: prompts/employee-C.md
    skills: [pnpm-vitest, playwright-e2e]

  manager:
    display: 管理员（语义验收+兜底决策）
    worker: { type: agent, backend: codex, model: gpt-5 }
    system_prompt: prompts/manager.md
    authority: semantic_verify_only          # ADR-006：无翻案权

resources:
  browser: { capacity: 2 }                   # V1
  expensive_model: { capacity: 1 }           # V1

budgets: { daily_usd: 30, max_parallel: 4 }
```

### 4.2 workflow.yaml + TaskPack

```yaml
# workflows/aideanbot-t7.yaml
name: aideanbot-3333-link
tasks:
  - pack: packs/A-T16.yaml        # 角色→包 在 pack 内声明，也可此处覆盖
    role: employee-A
  - pack: packs/B-T10.yaml
    role: employee-B
  - pack: packs/C-T7.yaml
    role: employee-C
    depends_on: [A-T16]
  - pack: packs/FINAL-ACCEPT.yaml
    role: manager
    kind: verify                   # 验收节点，非实现节点
    depends_on: [A-T16, B-T10, C-T7]
```

```yaml
# packs/C-T7.yaml —— TaskPack（Job Spec）
id: C-T7
title: 3333 真实链路
goal: 复用 http://localhost:3333 完成 SSO→建空间→202→READY→SSE

context_files:
  - frontend/lib/api.ts
  - frontend/components/AddArticlePanel.tsx
  - .docs/API接口文档.md

constraints:
  - "只允许 3333，严禁 3334（AideanBot 端口铁律）"
  - "NEXT_PUBLIC_API_MOCK=false"
  - "不改 backend/ docker/ globals.css"

workspace:
  read:  [backend/**, docs/**]
  write: [frontend/lib/**, frontend/components/**, frontend/app/**]
  deny:  [backend/**, docker/**, .env*, .git/**]

resources:
  exclusive: [port:3333]           # PortGate + Lease 依据

acceptance:                        # Command/Test Gate 的命令清单
  - cmd: netstat -ano | findstr :3333    # 必须 ≥1 行
  - cmd: netstat -ano | findstr :3334    # 必须为空（端口铁律）
  - cmd: pnpm test:unit             # exit 0, "43 passed"
  - cmd: pnpm typecheck             # exit 0
  - cmd: pnpm build                 # exit 0, "8/8"
  - screenshots: 3                  # URL 含 3333
  - eventstream_frames: 9           # meta/delta/done

output_contract:
  required_sections: [改动清单, 命令记录, 证据链, 四要素, 未完成事项]
  evidence_columns: [编号, 目标, 文件, 命令, 关键结果, 结论, 限制]

depends_on: []
max_rework: 3
```

### 4.3 事件模型（Event Bus）

存储：`runs/<run_id>/events.jsonl`，append-only，一行一事件；SQLite 表 `task_state` 为投影。

```json
{ "seq": 42, "ts": "2026-09-11T15:40:12+08:00", "run_id": "r-20260911-001",
  "task_id": "C-T7", "role": "employee-C",
  "type": "COMMAND_FINISHED",
  "data": { "cmd": "pnpm test:unit", "exit_code": 0, "evidence": "EV-CMD-018" } }
```

事件清单（初版）：`RUN_CREATED / TASK_READY / LEASE_ACQUIRED / LEASE_RELEASED / WORKTREE_CREATED / WORKER_STARTED / AGENT_EVENT / COMMAND_STARTED / COMMAND_FINISHED / FILE_CHANGED / RATE_LIMITED / RETRY_SCHEDULED / GATE_STARTED / GATE_FAILED / GATE_PASSED / VERIFY_STARTED / VERIFY_FINISHED / MERGED / TASK_SUCCEEDED / TASK_FAILED / TASK_BLOCKED / TASK_ESCALATED / RUN_FINISHED`

三职责分离（ADR-009）：`events.jsonl`=发生了什么（审计源）；`state.db`=现在在哪（调度依据）；`artifacts/`=证据在哪（验收依据）。

### 4.4 任务状态机（十态）

```text
PENDING ──(依赖满足+资源就绪)──→ READY ──(dispatch)──→ RUNNING
RUNNING ──(worker 提交)──→ VERIFYING ──(全门 PASS+验证 PASS+merge)──→ SUCCEEDED
VERIFYING ──(门 FAIL，可恢复)──→ RETRY_WAIT ──→ RUNNING（带 RetryContext）
VERIFYING ──(不可恢复/超上限)──→ FAILED
READY/RUNNING ──(外部资源被占)──→ BLOCKED
FAILED/重试耗尽 ──(需人工)──→ ESCALATED
任意 ──(用户取消)──→ CANCELLED
```

迁移即事件：每次状态变更写一条 `TASK_*` 事件，SQLite 同步更新——崩溃恢复时以事件流校验投影（事件为真，投影可重建）。

### 4.5 Evidence 模型

```text
artifacts/<run_id>/
├── EV-DIFF-007.patch            # git diff 原样
├── EV-CMD-018-pnpm-test.out     # 命令 stdout+stderr 原样（含失败）
├── EV-SCR-021.png               # 截图
├── EV-SSE-022.jsonl              # EventStream 帧原样
└── EV-AGENT-003.jsonl            # 基座流式事件原样
```

规则：只追加不修改；报告/验收只引用编号，不复述内容（防 LLM 转述失真）；`meta.yaml` 记录采集时间、采集者（role/gate）、关联命令。

### 4.6 Worker 协议与 Capabilities

```python
# core/workers/protocol.py
@dataclass
class WorkerResult:
    ok: bool
    output_path: str | None          # 指向 EV-AGENT-*.jsonl，不内联大文本
    error: ErrorInfo | None          # code: E429/E400/ETIMEOUT/ETOOL/...
    usage: Usage | None              # tokens/cost（如基座可报）

class AgentWorker(Protocol):
    backend: str
    caps: Capabilities
    def run(self, job: WorkerJob, on_event: Callable[[Event], None]) -> WorkerResult: ...
```

```python
Capabilities(streaming=True, structured_output=True, resume_session=False,
             mcp=True, native_skills=True, sandbox=True, usage_reporting=True)
```

Adapter 实现要点：

1. **流式**：`subprocess.Popen` + 逐行读 JSON 事件（claude `--output-format stream-json`、codex `--json`），实时回调 `on_event` 落盘——30 分钟任务期间进度可见，且 EventStream 帧直接成为证据；
2. **错误规范化**：各 CLI 的错误信封不同（退出码/stderr 文本/JSON 内 `is_error`），Adapter 内聚归一化为 `ErrorInfo`，禁止上层做子串匹配；
3. **超时清理**：Windows 下用 Job Object（`psutil` 亦可）保证杀掉整棵进程树，只杀自己启动的（ADR-008/FR-7.4）；
4. **契约实测**：编码前对每个基座跑 `--help/--version` + 一次 echo 冒烟，把实测参数固化进 Adapter 单测（d7 铁律：第三方文章不作契约）。

### 4.7 Gate 协议

```python
class Gate(Protocol):
    name: str
    def verify(self, task: Task, artifacts: list[Evidence],
               ctx: RunContext) -> GateResult: ...   # pass/fail + reason + evidence_refs

# coding 域门链（顺序固定，短路执行）
GATE_CHAIN = [DiffGate, PathGate, PortGate, CommandTestGate, ContractGate]
# 通用门
COMMON_GATES = [SchemaGate, BudgetGate, HumanApprovalGate]
```

| 门 | 检查内容 | 依据 |
|---|---|---|
| DiffGate | worktree 相对 base 有改动（防空提交） | git diff |
| PathGate | 改动文件 ⊆ taskpack.write 且 ∩ deny = ∅，越界整体打回 | git diff + glob |
| PortGate | exclusive 端口：应监听的有监听、严禁的无监听 | Lease + 实测探测 |
| CommandTestGate | acceptance 命令逐条执行，exit code 与期望匹配，输出原样归档 | subprocess |
| ContractGate | 员工输出含五节、证据表列齐全、无证据结论均标"待确认" | 解析输出 |
| HumanApprovalGate | 高危节点等人点确认（UI/CLI 阻塞或通知） | 审批 |

### 4.8 Resource Lease

```text
leases 表: resource='port:3333' | owner_run_id | owner_task_id | pid_tree | acquired_at | released_at
```

获取：探测空闲 → 写租约 → 启动前复核；释放：任务终态时按 pid_tree 清理自有进程并核销；启动时回收"进程已死但租约未释放"的泄漏项；**外部 PID 占用 → 事件 `TASK_BLOCKED(RESOURCE_BLOCKED)`，绝不 kill**（d7）。

### 4.9 Retry Context

```yaml
retry_context:
  previous_attempt: 2
  failure: { type: TEST_FAILURE, evidence: EV-CMD-018 }
  previous_diff: EV-DIFF-007
  instruction: "只修复上述失败，不扩大任务范围"
```

规则：重试 prompt = system + taskpack + retry_context，**绝不携带上一轮聊天历史**（几十 KB stdout 与对话回灌既贵又不稳定，d7）。

---

## 5. 关键流程设计

### 5.1 容错状态机（PolicyEngine，纯代码）

```python
def decide(st: TaskState, err: ErrorInfo, cfg: RoleCfg) -> Decision:
    match err.code:
        case "E429":
            if st.attempt < len(cfg.fallback_models):
                return Retry(model=cfg.fallback_models[st.attempt], wait=10 * 2**st.attempt)
            if st.queue_passes < 3:
                return QueueRetry(wait=60 * st.queue_passes)          # 指数退避
            if ctx.parallel:
                return DegradeSerial()                                  # 降级串行
            return Escalate(reason="429_exhausted")
        case "E400":
            if not st.trimmed:
                return RetrySameModel(mutations=[truncate_context, strip_media],
                                     note="裁剪后仅重试一次")
            return Escalate(reason="400_persisted", attach=err.raw)
        case "ETIMEOUT":
            return SplitTask(hint=err.last_action)                      # V1：先标记人工拆分
        case "ETOOL":
            return Rework(retry_context=build_retry_context(st, err))
        case "ERESOURCE_BLOCKED":
            return Block(detail=err.detail)                             # BLOCKED ≠ FAILED
    # 所有决策写《兜底记录表》事件
```

不变式：PolicyEngine 全程无 LLM 调用（ADR-005）；LLM（SemanticVerifier）只在门链之后与失败诊断两处出现。

### 5.2 断点恢复

```text
factory resume(run_id):
  1. 重放 events.jsonl → 重建/校验 state.db 投影
  2. SUCCEEDED/FAILED/ESCALATED → 不动
  3. RUNNING/VERIFYING → 标记 READY 重跑（worktree 若有未合改动词法保留则生成 diff 证据后重置）
  4. 清理孤儿租约/进程（仅自有 pid_tree）
  5. Scheduler 继续调度 READY 集
```

### 5.3 并发冲突处理

```text
运行前：对所有任务两两求 write_set 交集 → 非空者在 DAG 中加伪依赖（串行化），事件留痕
运行中：每任务独立 worktree（worktrees/<run_id>/<task_id>/，基于 main 建 task/<id> 分支）
验收后：Merge Gate 按依赖序 rebase 合并；冲突 → 冲突方 RETRY_WAIT + RetryContext(说明冲突文件)
```

### 5.4 语义验收（SemanticVerifier）

输入：任务包 acceptance 语义 + 证据编号索引 + 员工输出五节。输出（强制 JSON）：

```json
{ "verdict": "PASS|FAIL|NEEDS_REWORK", "per_criterion": [{"id": "AC-3", "result": "PASS",
  "evidence": "EV-SCR-021"}], "failure_diagnosis": "...", "rework_instruction": "...",
  "narrative": "供报告引用的一段验收叙述" }
```

硬约束：verdict 为 FAIL 时即使 LM 想 PASS 也无效（门链结果先行，ADR-006）；NEEDS_REWORK 触发 PolicyEngine 的 rework 分支。

---

## 6. 报告生成

模板：Jinja2 单页 HTML。数据源：events.jsonl + state.db + artifacts 索引（**零 LLM 参与事实段**；LLM 叙述仅引用 verifier 的 narrative 字段）。

六节结构：①各角色改动文件清单（来自 diff）②命令记录原样（含失败）③证据链表格（编号可点击展开 artifact）④四要素（截图/日志/EventStream/产物）⑤兜底记录表（PolicyEngine 决策流）⑥未完成事项（FAILED/ESCALATED/BLOCKED 明细）。

---

## 7. 安全与治理

| 机制 | 实现 |
|---|---|
| 权限最小化 | 角色路径 write/deny（PathGate 强制）+ 工具白名单（skills 声明） |
| 审计 | events.jsonl append-only，含完整决策链 |
| 预算 | BudgetGate：任务级/日级 USD 与并发上限，超限暂停 |
| 审批 | HumanApprovalGate：发布/删除/对外不可逆动作 |
| 密钥 | 环境变量注入子进程；事件/报告/证据中扫描并脱敏 |
| 进程安全 | 只清理自有 pid_tree；外部占用只报告 |

---

## 8. 技术选型

| 项 | 选择 | 理由 |
|---|---|---|
| 语言 | Python 3.11+ | subprocess 编排/YAML/重试逻辑顺手；ADR-002 |
| 状态 | SQLite（stdlib） | 单机零运维，投影可重建 |
| 事件 | JSONL 文件 | append-only、可 diff、可重放 |
| 并发 | ThreadPoolExecutor + subprocess | IO 型负载，无需 asyncio 全家桶 |
| 模板 | Jinja2 | report.html |
| UI | Streamlit（壳） | 三按钮+配置页，ADR-010 |
| 校验 | pydantic + JSON Schema | fleet/taskpack lint |
| 质量 | pytest + import-linter | 依赖方向铁律进 CI |
| 不用 | LangGraph/CrewAI/Mastra/OpenClaw | ADR-001/012 |

---

## 9. MVP 实施计划

```bash
# 环境
python -m venv .venv && .venv/Scripts/pip install pydantic pyyaml jinja2 streamlit psutil pytest import-linter
```

| 阶段 | 交付 | 验证命令 | 预计 |
|---|---|---|---|
| P0 协议冻结 | `fleet.schema.json`、`taskpack.schema.json`、events/state/gate/worker 协议代码 | `pytest core/` | 0.5 天 |
| P1 Adapter 冒烟 | 4 个基座 Adapter + Capabilities 探测 + echo 冒烟测试 | `factory doctor`（逐基座跑 `--version`+echo） | 1 天 |
| P2 串行引擎 | WorkflowEngine+StateStore+EventBus，单任务端到端 | `factory run packs/demo.yaml` | 1 天 |
| P3 门与报告 | GateEngine 五门 + ArtifactStore + report.html | 人为失败看打回与留证 | 1 天 |
| P4 并发与隔离 | Scheduler 冲突预检 + worktree + Lease | 双任务写同区自动串行 | 1 天 |
| P5 容错 | PolicyEngine + RetryContext + resume | mock 429/400 注入测试 | 1 天 |
| P6 UI | Streamlit 三按钮 + 配置页 | `streamlit run ui/app.py` | 0.5 天 |

P0–P6 完成后执行《需求文档》§8 的六项 MVP 验收场景。

## 10. 测试策略

1. **FakeAdapter**：确定性假基座（脚本化输出/可控故障），覆盖 90% 引擎逻辑测试，不花 token；
2. **故障注入**：`E429/E400/ETIMEOUT/ERESOURCE_BLOCKED` 各路径断言决策与《兜底记录表》；
3. **门单测**：每门独立（越界 diff、端口占用、契约缺节等夹具）；
4. **恢复测试**：运行中 kill 编排器 → resume 断言状态一致性；
5. **报告金样**：golden file 对比，防模板回归；
6. **依赖方向**：import-linter 进 CI（ADR-013）；
7. **真基座冒烟**：每基座一次 echo 级真调用，进 nightly（可选，控成本）。

## 11. 风险与缓解（设计层）

| 风险 | 缓解设计 |
|---|---|
| 基座 CLI 改版 | 错误归一化内聚 Adapter + Capabilities + doctor 命令 + 版本记录进证据 |
| LLM 验收幻觉 | 门链先行 + verdict 受门结果钳制 + 证据编号引用制 |
| 上下文爆炸 | RetryContext 结构化 + 400 裁剪一次 + 输出走文件不走 prompt |
| 端口/进程残留 | Lease + 自有 pid_tree 清理 + 启动期泄漏回收 |
| 同区并发写 | write_set 静态串行化 + worktree + Merge Gate 冲突返工 |
| 范围膨胀 | ADR-013/014 依赖方向与单 Domain 纪律；core 行数预算告警 |
| Windows 差异 | 进程管理抽象（Job Object/psutil）；路径统一 pathlib |

## 12. 与外部生态的集成边界

```text
本项目 Kernel ── Worker 协议 ──> claudecode / codex / opencode / pi（现在）
             ── Worker 协议 ──> 未来任何新 coding agent（加 Adapter 即可）
             ── MCP（V2）────> 工具生态
             ── P3 常驻层 ───> OpenClaw（运营/客服/渠道，作为外挂运行时而非地基）
             ── 观察名单 ───> Hermes Agent（自改进 skills 若成熟，可作 skill 供给方）
```

核心不变式：**基座会换、领域会扩，控制面的协议（TaskPack/Event/Gate/Lease/Evidence）保持稳定——这才是本项目真正的资产。**

# MCP 挂载·调用·报告（P0-2 验收记录）

| 项 | 内容 |
|---|---|
| 日期 | 2026-09-12 晚 |
| 结论 | **PASS**：MCP 工具在 Worker 端真实联网调用成功 |

## 1. 现状盘点（`hermes mcp list`）
| Server | 传输 | 状态 |
|---|---|---|
| chrome-devtools | npx chrome-devtools-mcp | enabled（未测） |
| sequential-thinking | npx server-sequential-thinking | ✅ test 通过（本地推理工具） |
| context7 | npx @context7/mcp-server | ✗ 连接失败（35s）→ **已从 7 个角色 profile 移除**（每次会话白耗 35 秒） |
| exa | npx exa-mcp-server（含 API Key） | ✅ test 通过（web_search_exa / web_fetch_exa） |

## 2. 挂载链路
MCP 配置位于各 profile 的 config.yaml（`mcp_servers:` 段，克隆自 Manager）→ 角色会话启动时自动连接 → 工具以 MCP 形式注册给该角色。

## 3. 实调证据（worker-a · SenseNova）
```
指令：请使用 MCP 的 web_search_exa 工具搜索 'Hermes Agent NousResearch GitHub'…
回复：搜索成功。第一条结果：
  标题：NousResearch/hermes-agent: The agent that grows with you
  URL：https://github.com/nousresearch/hermes-agent
```
（结果与真实仓库一致，证明是真实联网搜索而非模型幻觉）

## 4. 使用说明
- 全部 7 个角色 profile 均已具备 exa + sequential-thinking（联网/查询即"通用基础配置"的 MCP 部分）。
- 角色在任务中需要联网查资料时，管理者可在任务包中明示"允许使用 web_search_exa"。
- 注意：worker-b/be-2/be-3 的 .env 带 10808 代理（为 b.ai 设置），其 MCP 出网也走代理；代理不在线时这三人的外网能力同 受影响。
- context7 若将来修复（换官方包名或网络环境），可在 Manager profile 用 `hermes mcp add` 重新加回。

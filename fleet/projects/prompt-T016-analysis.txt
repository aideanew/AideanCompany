用 a2a_call 工具向 gpt-planner 发送下面内容（全文原样发送），等它回复后把回复原样转给我：【任务包】任务编号：T-016-ANALYSIS ｜ 目标：T-016 P0 资产缓存 现状分析与修复派工建议（gpt-6-astra 规划角色） ｜ 工作目录：E:/Code/AideanBot ｜ 要求与约束：【现状交底（管理者监督记录 2026-09-14）】T-016 此前由管理者代执行了一部分，当前工作区状态： ｜ ① entities.py 已加 ContentAsset.hit_count 列；② alembic 新迁移 ab1004p0a_p0_asset_hit_count.py（幂等加列+回滚 drop，PG 库已对齐：alembic_version=ab1004p0a，content_assets 表已有 hit_count 列）；③ AssetRepository 已加 hit() 与 mark_stale_docs()； ｜ ④ kb.py ingest_url 已改先查后抓（_find_source_by_anchor → READY 资产复用 hit+1 → 未命中抓取 → hash 比对 version+1 旧 doc superseded），响应已追加 hitCache/hitCount 字段；⑤ tests/test_p0_asset_cache.py 已建 3 个机器门用例（当前 3 failed）。 ｜ 【已知失败根因线索（供你分析）】test_repositories/test_langbot 等既有连库测试出现 users 表 ix_users_sub 唯一冲突——疑似 conftest 外层事务隔离与 Service 内 savepoint commit 语义交互问题，或 _make_user 固定 sub 在并行/连跑时残留；先跑单文件隔离复现，不要动 conftest 既有语义，优先定位 P0 改动引入面。 ｜ 【你的任务（分析+派工建议，不直接改业务代码）】 ｜ 1. 分析 tests/test_p0_asset_cache.py 三个失败 + test_langbot 既有回归的根因（读代码+跑命令，可修改测试写法但不改生产代码逻辑）；2. 给出派工清单：哪些修复/补全步骤派给 worker-b（9902），哪些派给 worker-a/fe-2（前端 hitCache toast 登记在 P1 阶段）；3. 输出文件：E:/Code/AideanCompany/fleet/projects/AideanBot/AB-P004/t016-analysis.md，内容=根因分析（带命令输出原文）+ 派工建议（步骤号/角色/验收命令/机器门 grep 令牌）+ 风险点。你可用 a2a_call 向 worker-b 探询现状（可选，非必须）；改测试文件允许，改 app/ 生产代码需在报告中登记。 ｜ 完成标准：t016-analysis.md 落盘且含 根因分析/派工建议/风险点 三节 ｜ 报告要求：按你的 SOUL.md 报告格式回复
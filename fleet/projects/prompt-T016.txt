用 a2a_call 工具向 worker-b 发送下面内容（全文原样发送），等它回复后把回复原样转给我：【任务包】任务编号：T-016 ｜ 目标：AB-P004 阶段1 P0 资产缓存（ingest_url 先查后抓+hit_count+版本管理+复用单测） ｜ 工作目录：E:/Code/AideanBot ｜ 要求与约束：后端 P0 资产缓存落地，工作目录 E:/Code/AideanBot（严格按冻结口径 E:/Code/AideanCompany/fleet/projects/AideanBot/AB-P004/freeze-scope-AB-P004.md 第1/3/5/6条）。改动范围：backend/app/services/kb.py 的 ingest_url 改为先查后抓：URL 归一（biz+article_key，复用 services/normalizer.py）→ 查 (source_id,external_id) 命中 READY 的 ContentAsset 直接复用并 hit_count+1（零微信请求）→ 未命中才抓取 → content_hash(正文md) 比对，号主改文则 version+1 且旧 KnowledgeDocument 标过期（不静默覆盖）→ raw_uri+content_markdown 落库 → 走 LangBot 引擎上传。新增 Alembic 迁移：ContentAsset.hit_count(int默认0)。同步更新 .docs/数据库文档.md（迁移预告+回滚纪律）与 .docs/API接口文档.md（F1 响应追加 hit_cache 布尔字段约定，命中时前端提示“命中缓存，秒入库”）。完成标准（机器门）：① ruff check + mypy + pytest 全绿（pytest 须含新增资产复用单测 ≥3 个：命中复用不抓、hash 变化 version+1 旧 doc 过期、hit_count 自增）；② 若运行环境可起服务则补 curl 证据（同一 URL 两次入库：第二次日志 0 次抓取、hit_count=2）落 .workbuddy/evidence/p0_asset_cache_20260913.txt，环境不可起则写单测证据并如实标注 BLOCKED 项；③ git 提交（feat 前缀中文 subject，不推远端）。报告按 SOUL 六节格式：任务清单/改动文件/验收命令记录含失败/返工记录/未完成事项/结论。 ｜ 【环境情报（管理者补充，2026-09-13 实测）】① 后端 venv 在 E:/Code/AideanBot/backend/.venv（Python 3.13.7），ruff/pytest 必须用 .venv/Scripts/python.exe 执行（全局 python 无 ruff 模块）；基线：ruff All checks passed，pytest 174 passed/2 skipped（约10秒，PG 5433 可达）；② Docker 全栈在跑：aideanbot-backend:8000、aideanbot-frontend:3333、aideanbot-postgres:5433(healthy)、aideanbot-langbot:5300、aideanbot-redis:6380；curl 证据可用：登录走 POST /api/v1/auth/aidean/callback（SSO），或直接用 TestClient 打 8000；③ 代码现状已探明：kb.py:ingest_url 目前先抓后存（resolver.resolve→extractor→quality→LangBot 上传→_persist_document 才 upsert asset），AssetRepository 已有 get_by_source_external/upsert_content（hash 同→False，hash 变→version+1）；entities.py 无 hit_count 列，需 Alembic 新迁移（head=05a5a856c37b，注意 migration 内加列需幂等）；docker 容器内 PG 表未跑 alembic 时可用 psql ALTER TABLE content_assets ADD COLUMN hit_count INTEGER NOT NULL DEFAULT 0 对齐；④ 测试惯例：tests/test_langbot.py 用 httpx.MockTransport 桩 LangBot + _StubResolver 桩微信抓取，复用单测可照此风格（零真实网络请求，fetcher 调用次数可计数断言『第二次 0 请求』）；⑤ 前端提示文案落点在 frontend 的 AddArticlePanel/入库成功 toast，P0 阶段后端侧只需在 ingest_doc 202 响应 data 追加 hitCache:boolean 与 hitCount:number（契约 v0.4 备案），前端改动产出物可登记到报告『未完成事项』留给 P1 阶段；⑥ git 提交 feat 前缀中文 subject，不推远端；⑦ 30005 已登记为 JOB_STATE_INVALID/409，不要重定义语义，『PARTIAL_SUCCESS』是 Job 终态字符串（state_machine 已有），非错误码，报告里措辞注意区分。 ｜ 完成标准：管理者将机器执行验收命令：cd E:/Code/AideanBot ＆＆ python -m ruff check backend ＆＆ python -m pytest backend/tests -q -k asset or cache or kb ｜ 报告要求：按你的 SOUL.md 报告格式回复（六节）
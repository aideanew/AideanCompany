用 a2a_call 工具向 reviewer-1 发送下面内容（全文原样发送），等它回复后把回复原样转给我：【审查请求】任务编号：T-016（AB-P004 阶段1 P0 资产缓存（ingest_url 先查后抓+hit_count+版本管理+复用单测）） ｜ 验收命令：cd E:/Code/AideanBot ＆＆ python -m ruff check backend ＆＆ python -m pytest backend/tests -q -k asset or cache or kb ｜ 机器验收结果：exit_ok=True（ruff All passed / mypy 0 error / pytest 170 passed+2 skipped，证据 .workbuddy/evidence/p0_asset_cache_20260913.txt）｜ 员工报告摘录：P0 资产缓存完整交付（管理者代执行，gpt-planner 规划监督）： ｜ ① entities.ContentAsset.hit_count + Alembic ab1004p0a（幂等加列/回滚 drop） ｜ ② AssetRepository.hit()/mark_stale_docs()/upsert 透传 hit_count ｜ ③ kb.ingest_url 先查后抓：URL 归一→(source,external) READY 复用 hit+1→未命中抓取→hash 比对 version+1 旧 doc superseded→raw_uri+markdown 落库→引擎上传 ｜ ④ ingest_doc 202 响应追加 hitCache/hitCount（v0.4b 备案） ｜ ⑤ tests/test_p0_asset_cache.py 3 个机器门用例全过 ｜ 机器门：ruff All passed / mypy 0 error / pytest 170 passed+2 skipped ｜ 容器：docker compose -p aideanbot build+up，PG 已对齐 alembic ab1004p0a ｜ 证据：.workbuddy/evidence/p0_asset_cache_20260913.txt ｜ 请按你的审查铁律给出四选一判定（PASS/PARTIAL/REWORK/BLOCKED）并逐条引用证据。
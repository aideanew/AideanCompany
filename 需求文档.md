# Agent Fleet 需求文档

| 项 | 内容 |
|---|---|
| 项目名 | Agent Fleet（工作名）—— 可配置的 AI 团队工作流执行系统 |
| 首个垂直场景 | 软件开发（Coding Fleet） |
| 版本 | v1.0（需求基线） |
| 日期 | 2026-09-11 |
| 状态 | 已冻结，进入协议与实现阶段 |
| 需求来源 | `初始设计/d0.md ~ d8.md`（每条需求标注来源） |

> 一句话定义（引自 d7）：Agent Fleet 是一个针对 Coding Agent 的、契约驱动（TaskPack）、事件溯源（Event Log）、资源隔离（Worktree/Lease）、策略恢复（Policy Engine）、证据验收（Machine Gate + Verifier）的轻量级执行控制面。

---

## 1. 背景与问题陈述

### 1.1 背景

用户维护长期软件项目（如 AideanBot，前后端分离、本地多端口协作），当前采用"员工 A/B/C + 管理员"的人工分工模式驱动多个 AI 编码工具（Claude Code / Codex / OpenCode 等）协作。该模式的问题：

1. **管理成本高**：需要反复追问、验收、协调端口与联调；
2. **上下文靠人搬运**：每个工具窗口的上下文由人复制粘贴，易漂移、易丢失；
3. **错误靠人兜底**：429 限流、400 上下文超长、超时等错误需要人工换模型、裁上下文、重跑；
4. **验收靠感觉**：员工（AI 工具）自称完成即算完成，无机器可验证的证据链；
5. **不可复现**：命令、模型版本、prompt 版本无统一留痕。

### 1.2 目标形态

用户只做两件事：

1. **开发前**：把文档准备充足（目标、上下文文件、验收标准、端口约束等），打包成结构化任务包；
2. **一键运行**：点一次按钮，中途无需问答，最后只接收结果报告。

中间过程全部由系统自动完成：多员工并行执行、错误自动兜底、机器验收、证据留痕、失败原样呈现。只有系统确实处理不了时才通知用户。

### 1.3 明确不是什么

- 不是"多个 AI 工具互相聊天"（d2/d3 已论证该模式必然失控）；
- 不是又一个 Agent 通信框架（LangGraph/CrewAI/Mastra 类，ADR-001 排除）；
- 不以"100% 永不需要人"为设计目标——正确目标是"**能自动化的自动化，不能可靠自动判定的工作自动升级**"（d8）。

---

## 2. 术语表

| 术语 | 定义 | 来源 |
|---|---|---|
| 基座 / Backend | 底层编码 Agent CLI，如 claudecode、codex、opencode、pi | d6 |
| Worker | 比"基座"更上层的执行者抽象：AgentWorker / ToolWorker / ServiceWorker / HumanWorker | d8 |
| 角色 / Role | Worker + 模型 + prompt + skills + 权限 + 预算的组合，如"员工A-后端" | d6/d8 |
| 任务包 / Task Pack（Job Spec） | 一个任务的结构化定义：目标、上下文、约束、验收、输出契约、资源 | d6/d8 |
| 输出契约 | 员工输出必须包含的章节结构（改动清单/命令记录/证据链/四要素/未完成事项） | d6 |
| Machine Gate | 机器验收门：diff/路径/端口/命令/契约等确定性检查，权威高于 Agent 自述 | d7 |
| Semantic Verifier | LLM 语义验收：只解释事实、给出返工建议，无权篡改事实层结果 | d7 |
| Evidence | 不可变的原始证据：命令 stdout/stderr 原样、截图、diff、EventStream 帧 | d3/d7 |
| Event Bus | 统一事件管道，append-only JSONL；正常事件与错误事件同管道 | d7 |
| Policy Engine | 确定性容错状态机：429/400/超时的换模型/退避/排队/拆分/升级 | d6/d7 |
| Resource Lease | 资源租约：端口等独占资源的归属记录（owner_run/pid/获取时间/释放策略） | d7 |
| Worktree | 每个并发写任务的独立 git 工作树，实现物理隔离 | d3/d7 |
| Retry Context | 结构化重试上下文（失败类型+证据编号+上次 diff+修正指令），非聊天历史 | d7 |
| 兜底记录表 | 系统自动处理错误时的完整留痕：时间/角色/错误码/动作/结果 | d6 |
| Domain Pack | 领域插件包（coding/media/research），内核不感知领域细节 | d8 |

---

## 3. 用户与场景

### 3.1 用户画像

- **U1 项目所有者（本人，MVP 唯一用户）**：熟悉 YAML/CLI，拥有多个编码工具订阅，管理 1~3 个长期项目；
- **U2 无经验用户（未来，商业化）**：不懂配置，购买"全包套餐"，依赖默认配置与引导（d0 商业模式：无经验用户直接购买；一人公司对外开放业务，平台收手续费）。

### 3.2 核心使用场景

**S1 一键多员工开发（MVP）**
准备 Task Pack → 点击运行 → 员工 A（claudecode+sonnet）改后端、员工 B（claudecode+haiku）跑契约测试、员工 C（opencode+qwen）做前端链路并行执行 → 期间 B 报 429，系统自动切备用模型重试 → 全部提交后机器门验收（端口 3333 有监听、3334 为 0、pytest 通过、输出契约五节齐全）→ 语义验收 → 生成 report.html → 用户只看报告。

**S2 失败与升级**
员工 C 的 e2e 失败 → 机器门拦截 → Policy Engine 生成 Retry Context（只修复该失败，不扩大范围）→ 自动返工（上限 N 次）→ 仍失败则标记 ESCALATED 并在报告"未完成事项"中呈现，附失败原样输出。

**S3 断点恢复**
运行中电脑重启 → `factory resume` → 系统从 SQLite 状态 + JSONL 事件恢复：已完成任务跳过、运行中任务重跑、等待中任务继续调度。

**S4 资源被占**
启动前发现 3333 被用户自己的服务占用 → 任务标记 BLOCKED（不是 FAILED），报告说明冲突 PID/进程，不杀外部进程。

**S5 自媒体团队（第二阶段，验证 Kernel 通用性）**
选题 Brief → 研究员 Agent 并行调研 → 作者 Agent 写稿 → 引用门/敏感词门/品牌门 → 主编 Agent 审核 → 人工审批 → 发布包。与 S1 共用同一 Kernel，不修改 core（d8 的"架构单元测试"）。

### 3.3 非目标用户

- 需要实时代码补全的 IDE 用户（用 Cursor 即可）；
- 需要常驻对话助理的用户（那是 OpenClaw 的领域，见 §10）。

---

## 4. 范围

### 4.1 MVP（第一阶段：Coding Fleet）

**包含**：fleet.yaml 配置体系、任务包、DAG 执行引擎、3+1 基座适配（claudecode/codex/opencode/pi）、Policy Engine 容错、机器门验收、语义验收、worktree 隔离、端口租约、事件溯源与状态持久化、report.html、Streamlit 三按钮 UI、断点恢复。

**不包含**：
- 自媒体/研究 Domain Pack（第二阶段）；
- AI 组织运行时/公司账户/付款审批/跨公司协议（第三阶段及以后，d0/d4 的拓展项）；
- DID/ANP 去中心化身份（字段预留，不实现）；
- 云端多机分布（单机 Windows 优先）；
- 自研模型、Agent 间自由聊天。

### 4.2 演进路线（d8 三阶段）

| 阶段 | 交付物 | 验证目标 |
|---|---|---|
| P1 Coding Fleet | 本文档 MVP | 核心引擎稳定性 |
| P2 Media Fleet | media Domain Pack + ToolWorker/ServiceWorker 完整化 | 同一 Kernel 不改 core 跑通第二领域（抽象正确性） |
| P3 AI Organization Runtime | 常驻型业务（运营/客服/监测）、渠道接入（可评估 OpenClaw 挂载）、组织级治理 | One-Company 愿景落地 |

---

## 5. 功能需求

优先级标记：**M** = MVP 必须，**V1** = MVP 后第一个迭代，**V2** = 第二阶段。

### FR-1 配置管理

| 编号 | 需求 | 优先级 | 来源 |
|---|---|---|---|
| FR-1.1 | 组织配置文件（fleet.yaml/organization.yaml）：定义角色列表，每个角色可独立配置基座+模型+备用模型链+system prompt 文件+skills 列表+超时+重试+预算+路径权限 | M | d6/d8 |
| FR-1.2 | 同一基座允许配不同模型（如员工A=claudecode+sonnet、员工B=claudecode+haiku），实现成本与配额隔离 | M | d6 |
| FR-1.3 | 基座可扩展：新增基座 = 实现 BackendAdapter 并注册，不改核心代码 | M | d6/d7 |
| FR-1.4 | Adapter 具备能力发现（Capabilities：streaming/structured_output/resume_session/mcp/sandbox/usage_reporting），核心不假设各 CLI 能力一致；编码前以本地 `--help/--version` 实测为准，第三方文章不得作为 Adapter 契约 | M | d7 |
| FR-1.5 | UI 提供配置页可直接编辑配置文件并校验（JSON Schema 校验） | V1 | d6 |
| FR-1.6 | Workflow 与组织定义分离（organization.yaml + workflow.yaml 两个文件），支持同一组织跑不同流程 | V1 | d8 |
| FR-1.7 | 配置版本化：prompt/skills/配置的版本号进入运行记录，保证可复现 | M | d3/d6 |

### FR-2 任务包（Task Pack / Job Spec）

| 编号 | 需求 | 优先级 | 来源 |
|---|---|---|---|
| FR-2.1 | 每任务一个结构化 YAML：id/title/goal/context_files/constraints/acceptance/output_contract/depends_on | M | d6 |
| FR-2.2 | workspace 声明：read/write/deny 三组路径 glob | M | d7 |
| FR-2.3 | resources 声明：独占资源（如 port:3333） | M | d7 |
| FR-2.4 | 输出契约：必含五节——改动清单、命令记录、证据链表格（编号/目标/文件/命令/关键结果/结论/限制）、四要素（截图/日志/EventStream/产物）、未完成事项 | M | d6 |
| FR-2.5 | Pack Lint：运行前校验任务包完整性（必填节、路径合法、acceptance 可执行），不合格直接拒绝运行并提示 | M | d7 启发 |
| FR-2.6 | 预算字段：单任务 token/USD 上限，超限自动暂停 | V1 | d0 |

### FR-3 一键执行引擎

| 编号 | 需求 | 优先级 | 来源 |
|---|---|---|---|
| FR-3.1 | 支持串行/并行/DAG 调度：无依赖任务自动并行，有依赖自动等待，完成后自动解除阻塞 | M | d1/d2 |
| FR-3.2 | 静态冲突预检：运行前计算两两 write_set 交集，非空则自动改串行并告知，而不是等 merge 冲突 | M | d7 |
| FR-3.3 | 断点恢复：`factory resume` 从持久化状态继续（completed 跳过、running 重跑、waiting 续跑） | M | d0/d3 |
| FR-3.4 | 取消：`factory cancel` 优雅终止（清理自有进程树与租约） | V1 | d3 |
| FR-3.5 | 任务状态机九态：PENDING/READY/RUNNING/VERIFYING/RETRY_WAIT/SUCCEEDED/FAILED/BLOCKED/ESCALATED/CANCELLED；BLOCKED 与 FAILED 必须区分 | M | d7 |
| FR-3.6 | 进度可见：运行中实时展示各任务状态与流式日志尾部 | M | d6 |

### FR-4 员工执行约束

| 编号 | 需求 | 优先级 | 来源 |
|---|---|---|---|
| FR-4.1 | 员工不许反问、不许扩大范围（prompt 层约束 + 越界 diff 打回的机器层约束，双层） | M | d6 |
| FR-4.2 | 所有结论必须有命令输出证据，无证据标"待确认" | M | d6 |
| FR-4.3 | 输出必须符合输出契约，缺节即不合格（机器判定） | M | d6 |
| FR-4.4 | 错误上报格式化：ERROR_CODE/ERROR_MSG/LAST_ACTION，禁止员工自己无限重试 | M | d6 |
| FR-4.5 | 端口铁律写入任务包并在验收中体现：Aidean 只能 3000、AideanBot 只能 3333、严禁 3334 | M | d6 |

### FR-5 容错与兜底（Policy Engine）

| 编号 | 需求 | 优先级 | 来源 |
|---|---|---|---|
| FR-5.1 | 统一错误分类：429/400/TIMEOUT/TOOL_FAIL/RESOURCE_BLOCKED/TEST_FAILURE | M | d6/d7 |
| FR-5.2 | 429 策略链：换备用模型 → 指数退避排队 → 降级串行 → 通知用户 | M | d6 |
| FR-5.3 | 400 策略链：自动裁剪上下文/修复参数重试一次 → 仍失败转人工并附原始错误全文 | M | d6 |
| FR-5.4 | TIMEOUT 策略：拆任务重跑 | V1 | d6 |
| FR-5.5 | 兜底逻辑必须是确定性代码（Policy Engine），Manager LLM 不处理机械重试 | M | d7 |
| FR-5.6 | 重试携带结构化 Retry Context（previous_attempt/failure.type/evidence 编号/previous_diff 编号/修正指令），不携带聊天历史 | M | d7 |
| FR-5.7 | 《兜底记录表》：时间/角色/错误码/动作/结果，进入报告 | M | d6 |
| FR-5.8 | 资源被外部占用 → BLOCKED + RESOURCE_BLOCKED 详情，不杀非自有进程 | M | d7 |
| FR-5.9 | 返工循环上限（默认 3 次可配），超限 ESCALATED | M | d6 |

### FR-6 验收体系

| 编号 | 需求 | 优先级 | 来源 |
|---|---|---|---|
| FR-6.1 | Machine Gate 链先于一切 LLM 验收：Diff Gate → Path Gate → Port Gate → Command/Test Gate → Artifact Contract Gate | M | d7 |
| FR-6.2 | Machine Gate 权威高于 Agent 自述：LLM 可以解释失败原因，不能把 `pytest exit_code=1` 解释成 PASS | M | d7 |
| FR-6.3 | Port Gate 实测验证（监听状态），不只依赖 prompt 承诺 | M | d7 |
| FR-6.4 | Semantic Verifier（Manager LLM）职责限定：读证据→判断达标→定位失败原因→给返工建议→写验收叙述 | M | d6/d7 |
| FR-6.5 | 需求追踪：REQ→Task→Evidence 三级编号关联，报告可回答"每条验收标准是否有实现与测试证据" | V1 | d3 |
| FR-6.6 | Gate 插件化（Gate Protocol），MVP 交付 coding 域门 + 通用门（SchemaGate/BudgetGate/HumanApprovalGate） | M(协议)/V1(全) | d8 |

### FR-7 隔离与资源管理

| 编号 | 需求 | 优先级 | 来源 |
|---|---|---|---|
| FR-7.1 | 每个并发写任务使用独立 git worktree + 分支；完成并通过验收后合并 | M | d3/d7 |
| FR-7.2 | 合并前 diff 对照任务包 write 白名单，越界改动整体打回 | M | d7 |
| FR-7.3 | Resource Lease：端口资源记录 owner_run_id/owner_task_id/pid/process-group/acquired_at/release policy | M | d7 |
| FR-7.4 | 系统只清理自己启动的进程树（Windows Job Object / taskkill /T 限定），防止残留进程占用端口污染下次运行 | M | d7 |
| FR-7.5 | 租约泄漏检测：启动时发现无主租约（进程已死）自动回收 | V1 | d7 启发 |

### FR-8 证据与报告

| 编号 | 需求 | 优先级 | 来源 |
|---|---|---|---|
| FR-8.1 | 事件溯源三分离：events.jsonl（发生了什么，append-only）/ SQLite（现在在哪，投影）/ artifacts/（证据在哪） | M | d7 |
| FR-8.2 | 证据不可变、raw-first：命令输出（含失败）原样保存，不加工不美化 | M | d3/d6 |
| FR-8.3 | report.html 由模板从事实数据生成，LLM 只写结论叙述段；禁止让 LLM 凭记忆复述结果 | M | d6/d7 |
| FR-8.4 | 报告必含六部分：各角色改动文件清单、命令记录（含失败原样）、证据链表格、截图/EventStream/产物索引、兜底记录、未完成事项 | M | d6 |
| FR-8.5 | 可复现记录：每个任务记录模型版本、基座版本、prompt 版本、命令清单 | M | d6 |

### FR-9 用户界面

| 编号 | 需求 | 优先级 | 来源 |
|---|---|---|---|
| FR-9.1 | 三个按钮：[一键运行] [查看报告] [重试失败项] | M | d6 |
| FR-9.2 | 配置页：可视化编辑 fleet.yaml | V1 | d6 |
| FR-9.3 | 核心代码不得依赖 UI 框架（UI 只是 CLI 之上的壳） | M | d7 |

### FR-10 治理（d0 治理层的技术化落地）

| 编号 | 需求 | 优先级 | 来源 |
|---|---|---|---|
| FR-10.1 | 预算池：角色级/任务级/日级 USD 与 max_parallel 上限，超限自动暂停 | V1 | d0 |
| FR-10.2 | HumanApprovalGate：对外不可逆动作（发布/删除/付款类）必须人工批准后继续 | V1 | d0 |
| FR-10.3 | 审计：append-only 事件日志即为审计底座（谁/何时/何任务/何命令/何结果） | M | d0 |
| FR-10.4 | 权限最小化：角色路径白名单 + deny 列表 + 工具白名单 | M | d0 |

---

## 6. 非功能需求

| 编号 | 需求 | 来源 |
|---|---|---|
| NFR-1 可复现 | 相同任务包+相同配置+相同基座版本，重跑结果可对比（命令/版本/prompt 全留痕） | d6 |
| NFR-2 不伪造 | 测试失败原样保留；报告事实段零 LLM 参与；"测试通过≠项目正确"（SWE-bench+ 教训），故需多门+语义双层验收 | d3 |
| NFR-3 成本可控 | 便宜模型跑测试、贵模型跑推理（同基座不同模型隔离配额）；pi 可接免费/本地模型 | d6 |
| NFR-4 平台 | Windows 10/11 + Git Bash 优先（用户环境），代码跨平台（进程管理抽象） | 环境事实 |
| NFR-5 性能 | MVP 支持 3~4 并发 Worker；单机 | d6 |
| NFR-6 健壮性 | 编排器自身崩溃不丢状态（每步先落盘再执行）；孤儿进程可回收 | d0/d7 |
| NFR-7 可扩展 | 新基座/新门/新领域均以插件进入，不改 core（依赖方向铁律） | d8 |
| NFR-8 安全 | API 密钥只经环境变量注入，不进配置文件/报告/事件日志；报告可对外分享前可脱敏 | d0 治理 |
| NFR-9 可维护规模 | MVP 控制面代码 ≤ ~3000 行 Python，超出视为抽象失败信号 | d7 |

---

## 7. 硬约束（铁律）

1. **端口铁律**：Aidean 只能 3000；AideanBot 只能 3333；严禁 3334。写入任务包 constraints、Machine Gate Port Gate、报告端口检查节三层。
2. **不允许多员工同时改同一核心区**：write_set 静态预检 + worktree 物理隔离 + diff 越界打回三层防线。
3. **不伪造测试结果**：失败原样输出进报告；Machine Gate 结果不可被 LLM 翻案。
4. **员工零反问**：任务包上下文必须在运行前给足（Pack Lint 保证），运行中员工无求助通道，只能成功、失败或上报错误码。

---

## 8. MVP 验收标准（Exit Criteria）

以下场景全部可演示则 MVP 完成：

1. 对 AideanBot 真实小任务（如 3333 真实链路联调）一键运行：三角色并行 → DAG 依赖正确 → report.html 六节齐全；
2. 人为制造 429（mock）：系统自动换备用模型重试成功，兜底记录表有痕；
3. 人为制造验收失败（e2e fail）：机器门拦截 → Retry Context 自动返工一次 → 通过；
4. 运行中 `taskkill` 编排器：`factory resume` 后从断点继续，已完成任务不重跑；
5. 端口被外部进程占用：任务 BLOCKED（非 FAILED），报告给出占用详情，未杀外部进程；
6. 员工越界写 deny 路径：diff 打回，报告中留证。

---

## 9. 风险与开放问题

| 风险 | 影响 | 缓解 |
|---|---|---|
| 上游 CLI 变动（参数/输出格式改版） | Adapter 失效 | Capabilities 探测 + 版本锁定记录 + Adapter 单测 + 错误分类容错 |
| 任务包质量差 → 全自动放大错误 | 返工循环烧钱 | Pack Lint 强制完整性 + 返工上限 + 预算门 |
| 上下文爆炸（长任务/大 diff） | 400 频发 | Retry Context 结构化（不带聊天史）+ 400 裁剪策略 |
| Windows 进程树残留占端口 | 下次运行端口门误报 | Job Object/taskkill /T + 启动前租约检测 |
| 范围膨胀（One-Company 陷阱） | 项目失败 | 三阶段路线 + ADR 冻结 + MVP 行数上限 |
| 多基座 skills 冲突/语义不一 | 行为漂移 | skills 按 domain 命名空间隔离 + 版本号 |
| LLM 验收幻觉 | 假阳性验收 | Machine Gate 权威规则（ADR-006） |

开放问题（实现期决策，不阻塞需求）：
1. codex/opencode/pi 的无头 JSON 输出与错误信封的实测契约（以本地 `--help` 为准）；
2. 截图采集方案（Playwright headless 由谁触发：员工 or Machine Gate）；
3. 合并策略细节（rebase vs merge，冲突时是否自动请求返工）。

---

## 10. 与外部系统的关系边界

| 系统 | 关系 | 决策 |
|---|---|---|
| OpenClaw | 常驻助理网关（渠道/cron/skills 生态），与本项目"无头批处理控制面"不同层 | 不进入核心依赖（ADR-012）；保留为 P3 阶段运营/客服层的候选运行时 |
| Hermes Agent | 自改进单体 Agent，多 Agent 在路线图上 | 观察名单，不依赖 |
| pi | 极简模型无关编码 Agent | 作为第四个 Worker Backend 接入（便宜模型跑测试） |
| MCP | 工具协议 | 未来工具接入通道，不替代内部 Scheduler/Event Bus（ADR-011） |
| LangGraph/CrewAI/Mastra/MetaGPT 等 | Agent 编排框架 | 不用作核心（ADR-001），思想已吸收 |

---

## 附录 A：需求追踪矩阵（需求 ← 来源文档）

| 来源 | 贡献的核心需求 |
|---|---|
| d0 | FR-10 治理（预算/审批/审计/权限）、NFR-6、三阶段愿景、商业模式（U2） |
| d1 | §1 问题陈述、FR-3.1（并行+验收循环）、"全自动"目标定义 |
| d2 | FR-2（任务协议）、FR-3.5（状态机）、FR-6（验收）、SSOT/契约先行思想 |
| d3 | FR-6（证据驱动/追踪矩阵）、FR-7.1（worktree）、FR-8.2（raw-first）、NFR-2、Worker/Orchestrator 分层 |
| d4 | §3.2 S5（自媒体场景）、P2/P3 路线动机、§10 边界（客服/运营归 OpenClaw 层） |
| d5 | 排除法依据（无框架满足证据/门/租约一等公民）→ ADR-001 |
| d6 | FR-1/2/4/5/8/9 的具体形态（fleet.yaml/TaskPack/错误策略/报告三按钮） |
| d7 | FR-5.5/5.6/5.8、FR-6.1/6.2、FR-7.2~7.4、FR-8.1/8.3、FR-3.2/3.5、FR-1.4、全部 ADR |
| d8 | FR-6.6（Gate 插件化）、Worker 抽象、Domain Pack、三阶段路线、§1.3 边界声明 |
